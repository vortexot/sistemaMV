/** All durations in milliseconds. Visual colors and layout live in index.css. */
export const INDOOR = {
  intervalMs: 5000,
  transitionMs: 700,
  easing: "easeInOut" as const,
  refreshMs: 60000,
  recommendedWidth: 1920,
  recommendedHeight: 1080,
};
